d3.line.interpolate();
d3.area.interpolate();

d3.area().interpolate("linear");
d3.area().interpolate("linear-closed");
d3.area().interpolate("step");
d3.area().interpolate("step-before");
d3.area().interpolate("step-after");
d3.area().interpolate("basis");
d3.area().interpolate("basis-open");
d3.area().interpolate("basis-closed");
d3.area().interpolate("bundle");
d3.area().interpolate("cardinal");
d3.area().interpolate("cardinal-open");
d3.area().interpolate("cardinal-closed");
d3.area().interpolate("monotone");
