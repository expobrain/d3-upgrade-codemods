d3.line.curve();
d3.area.curve();

d3.area().curve(d3.curveLinear);
d3.area().curve(d3.curveLinearClosed);
d3.area().curve(d3.curveStep);
d3.area().curve(d3.curveStepBefore);
d3.area().curve(d3.curveStepAfter);
d3.area().curve(d3.curveBasis);
d3.area().curve(d3.curveBasisOpen);
d3.area().curve(d3.curveBasisClosed);
d3.area().curve(d3.curveBundle);
d3.area().curve(d3.curveCardinal);
d3.area().curve(d3.curveCardinalOpen);
d3.area().curve(d3.curveCardinalClosed);
d3.area().curve(d3.curveMonotoneX);
